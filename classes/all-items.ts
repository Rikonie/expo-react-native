class ItemListRow {
    id: number;
    id_group: number;
    title: string;

    constructor(id: number, id_group: number, title: string) {
        this.id = id;
        this.id_group = id_group;
        this.title = title

    }
}